,<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Ürün Kataloğu</title>
    <style>
        #camera-container {
            width: 100%;
            height: 300px;
            background: black;
            display: none;
            /* Başlangıçta gizli */
        }
    </style>
</head>

<body>
    <div class="container my-5">
        <h1 class="text-center">Stok Otomasyon Takip Sistemi</h1>
        <h2 class="mt-4 mb-4 text-center">Ürün Kataloğu</h2>

        <form action="/catalog" method="POST" class="border p-4 rounded shadow" onsubmit="showAlert(event)"> 
            <div class="mb-3">
                <label for="barkod" class="form-label">Barkod:</label>
                <input type="text" id="barkod" name="barkod" class="form-control" required>
            </div>
        
            <div class="mb-3">
                <label for="aciklama" class="form-label">Açıklama:</label>
                <input type="text" id="aciklama" name="aciklama" class="form-control" required>
            </div>
        
            <button type="submit" class="btn btn-primary">Ürün Ekle</button>
        </form>


        <h3 class="mt-4">Mevcut Ürünler</h3>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Barkod</th>
                    <th>Açıklama</th>
                </tr>
            </thead>
            <tbody>
                <% catalog.forEach(item=> { %>
                    <tr>
                        <td>
                            <%= item.barkod %>
                        </td>
                        <td>
                            <%= item.aciklama %>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>

        <!-- Ana Sayfaya Dönme Butonu -->
        <div class="text-center mt-4">
            <a href="/" class="btn btn-secondary">Ana Sayfaya Dön</a>
        </div>
    </div>

    


    <!-- Bootstrap JS ve popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

   
</body>

</html>